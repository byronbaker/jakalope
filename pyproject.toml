[project]
name = "jakalope"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Byron Baker", email = "byron@madlounge.com" }
]
requires-python = ">=3.9"
dependencies = []

[project.scripts]
jakalope = "jakalope:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
